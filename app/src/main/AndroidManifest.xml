<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.begentgroup.samplebasicwidget">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LinearActivity" />
        <activity android:name=".RelativeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GridActivity" />
        <activity android:name=".TextActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".adioActivity" />
        <activity
            android:name=".EdTextActivity"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".ProgActivity" />
        <activity android:name=".ProgrActivity"></activity>
    </application>

</manifest>